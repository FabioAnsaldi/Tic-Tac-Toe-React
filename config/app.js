'use strict';

/**
 *  The main paths of your project handle these with care
 */
exports.paths = {
    configuration: 'config',
    source: 'src',
    build: 'build',
    assets: '/',
    bundle: 'bundle.js'
};

